# Memory Palace is a local-first app; keeping the pnpm store inside the repo makes
# installs self-contained and avoids touching (or depending on) a global store.
store-dir=.pnpm-store
# pnpm is strict about peer deps by default; we'd rather see warnings than a broken install.
strict-peer-dependencies=false
